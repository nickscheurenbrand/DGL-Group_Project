# ==========================================
# Dataset Configuration: Brain Graph Super-Resolution
# ==========================================
name: "SLIM_Functional_Connectomes"

# --- Data Splits ---
# Total number of subjects is 279
# Split proportions are 0.6 (Train), 0.2 (Public Test), 0.2 (Private Test)
n_samples_total: 279
n_samples_train: 167       # Used for 3-fold cross-validation
n_samples_test: 112        # 56 public + 56 private

# --- For Dummy Data Generation (if still using load_dataset mock) ---
n_samples: 167             # Defaulting to the training set size for the cross-validation loop

# --- Graph Dimensions (Matrix/Anti-vectorised format) ---
n_source_nodes: 160        # Number of brain regions in Low-Resolution (LR) graphs
n_target_nodes: 268        # Number of brain regions in High-Resolution (HR) graphs

# --- Feature Dimensions (Vectorised format for Kaggle) ---
# Extracted from the off-diagonal upper triangular part of the matrices
source_vector_size: 12720  # LR feature vector size
target_vector_size: 35778  # HR feature vector size

# --- File Paths (Update these to match your local structure) ---
data_dir: "/home/temi/ICL/deep_graph_learning/cw2/DGL-Group_Project/data/dgl-2026-brain-graph-super-resolution-challenge/"
test_lr_file: "lr_test.csv"
train_hr_file: "hr_train.csv"
train_lr_file: "lr_train.csv"